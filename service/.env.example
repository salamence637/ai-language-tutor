# OpenAI API Key for STT, LLM, and TTS
# Get your key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=
# Optional: Database path (default: ./aitutor.db)
# DATABASE_PATH=./aitutor.db

# Optional: Maximum audio length in seconds (default: 30)
# MAX_AUDIO_LENGTH_SECONDS=30

# Optional: Comma-separated CORS origins (default: localhost)
# ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Optional: Cookie settings for production (cross-site)
# COOKIE_SAMESITE=none
# COOKIE_SECURE=true
